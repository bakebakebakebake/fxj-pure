<%*
const fallbackTitle =
  tp.file.title && !["Untitled", "模板"].includes(tp.file.title) ? tp.file.title : ""
const title = (await tp.system.prompt("文章标题", fallbackTitle, true))?.trim() || fallbackTitle || "未命名文章"
const description =
  (await tp.system.prompt("文章简介", "", true))?.trim() || "待补充文章摘要"
const language = (await tp.system.suggester(["中文", "English"], ["中文", "English"], false, "文章语言")) || "中文"
const draft = (await tp.system.suggester(["false", "true"], [false, true], false, "是否为草稿？")) ?? false
const comment = (await tp.system.suggester(["true", "false"], [true, false], false, "是否开启评论？")) ?? true
const tagInput = (await tp.system.prompt("标签（可选，逗号分隔）", "")) || ""
const tags = [...new Set(tagInput.split(",").map((tag) => tag.trim()).filter(Boolean))]
const heroImagePath = ((await tp.system.prompt("封面图相对路径（可选，如 ./thumbnail.jpg）", "")) || "").trim()
const heroImageAlt = heroImagePath
  ? ((await tp.system.prompt("封面图 alt（可选）", "")) || "").trim()
  : ""
const heroImageColor = heroImagePath
  ? ((await tp.system.prompt("封面主色（可选，如 #B4C6DA）", "")) || "").trim()
  : ""

const q = (value) => JSON.stringify(value)

tR += "---\n"
tR += `title: ${q(title)}\n`
tR += `description: ${q(description)}\n`
tR += `publishDate: ${q(tp.date.now("YYYY-MM-DD"))}\n`
tR += "tags:"
if (tags.length) {
  tR += "\n"
  for (const tag of tags) {
    tR += `  - ${q(tag)}\n`
  }
} else {
  tR += " []\n"
}
tR += `language: ${q(language)}\n`
tR += `draft: ${draft}\n`
tR += `comment: ${comment}\n`
if (heroImagePath) {
  tR += "heroImage:\n"
  tR += `  src: ${q(heroImagePath)}\n`
  if (heroImageAlt) tR += `  alt: ${q(heroImageAlt)}\n`
  if (heroImageColor) tR += `  color: ${q(heroImageColor)}\n`
}
tR += "---\n"
-%>

> [!note] 说明
> <% tp.file.cursor(1) %>

## 收录内容

- 

## 正文
